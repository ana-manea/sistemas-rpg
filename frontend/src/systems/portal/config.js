import { portalTheme } from './theme.js';
import { portalRoutes } from './routes.js';
import { portalPages } from './pages.js';

export const portalSystem = {
  id: 'portal',
  name: 'Arquivo de RPG',
  shortName: 'Arquivo de RPG',
  basePath: '/',
  theme: portalTheme,
  routes: portalRoutes,
  pages: portalPages,
};
